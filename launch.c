#include <stdlib.h>

int main() {
  // PERSISTENT TERMINAL
  // system("gnome-terminal -- bash -c './hello; exec bash'");
  
  // TEMPORARY TERMINAL
  system("gnome-terminal -- bash -c './hello'");
  
  return 0;
}
